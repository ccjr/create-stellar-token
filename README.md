# create-stellar-token

This script will create a new token in the Stellar testnet. Very early version.

To name your token, change the `TOKEN_NAME` constant in `token.rb`.
To specify a total circulation amount, change the `TOTAL_CIRCULATION` constant in `token.rb`.

## Improvements

* Accept token, circulation, keys as parameter
